name: infrastructure
description: Infrastructure package containing persistence models, Drift database, and repository implementations.
version: 1.0.0
publish_to: none

resolution: workspace

environment:
  sdk: '>=3.11.0 <4.0.0'

dependencies:
  core:
  domain:
  drift: ^2.24.0
  drift_flutter: ^0.3.1
  path_provider: ^2.1.6

dev_dependencies:
  drift_dev: ^2.24.0
