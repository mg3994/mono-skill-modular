name: infrastructure
description: Data persistence and database infrastructure for BlogStore.
version: 1.0.0
publish_to: none

resolution: workspace

environment:
  sdk: '>=3.11.0 <4.0.0'

dependencies:
  drift: ^2.24.0
  drift_flutter: ^0.3.0
  path_provider: ^2.1.6

dev_dependencies:
  drift_dev: ^2.34.0
  flutter_test:
    sdk: flutter
