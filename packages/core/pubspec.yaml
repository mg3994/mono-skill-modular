name: core
description: Pure Dart core library containing base types, contracts, and configuration.
version: 1.0.0
publish_to: none

resolution: workspace

environment:
  sdk: '>=3.11.0 <4.0.0'

dependencies:

dev_dependencies:
