name: l10n
description: Localization package for app strings and delegates.
version: 1.0.0
publish_to: none

resolution: workspace

environment:
  sdk: '>=3.11.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: any

dev_dependencies:
  flutter_test:
    sdk: flutter
